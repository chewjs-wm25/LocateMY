---
kind: domain-context
language: zh-CN
canonical: true
---

# LocateMY 领域词汇

本文件只记录已确认的产品术语；功能公式和数据集事实分别以产品知识库的功能文件为准。

## 地点分析范围

一个地点由用户选择的坐标确定。公共交通覆盖范围是该坐标周围 1.5 公里的固定半径，周边设施覆盖范围是该坐标周围 2 公里的固定半径；治安使用地点所属统计州，地区统计使用地点所属行政区。

## 行政区与统计州

行政区（district）是收入、供水、供电、医疗、教育等地区统计使用的行政统计单位；统计州（reporting state）是治安统计使用的州级口径。治安结果按地点所属统计州聚合 `crime_district` 的警区原始记录，不解析警区边界；分析结果必须标明实际口径。

## 行政地理语境

**行政地理语境（administrative geographic context）**：由合法地点坐标按版本化行政区边界解析出的统计州和行政区事实。坐标被一个候选覆盖时为 resolved；零个候选为 unresolved；多个候选（含边界点或重叠）为 ambiguous，并完整保留候选集，不选择最近或默认地区。

## 周边设施覆盖

周边设施覆盖是 2 公里范围内已收录 OSM 设施的类别和数量读数，不是设施质量、营业状态或政府服务水平评价。覆盖类别未知时不能当作未覆盖。

## 公共交通覆盖

公共交通覆盖是站点和有效服务路线的相对覆盖读数，不是实际通勤便利或服务质量评价。交通连通性分数供公共交通页面和 ICI 共用。

## 站点实体

通常指 GTFS `stops.txt` 中可上下车的 `location_type = 0` 记录。`parent_station` 可用于展示分组，但不改变站点实体计数。

## 有效路线

通过 `routes.txt`、`trips.txt` 和 `stop_times.txt` 关联到分析范围内站点，并在分析日期由 `calendar.txt` 或 `calendar_dates.txt` 确认有效的路线。

## GTFS Feed 快照

某次取得的官方 GTFS Static ZIP、采集时间、解析状态和有效服务日期。可读取但较旧的快照仍可展示，并标记可能过期；解析失败或预期 feed 缺失时，交通分不可用。

## 政府数据镜像表

Supabase 中供应用读取的政府数据集副本。其业务列、字段类型和主键规则与相应政府数据集的 SQL 结构一致；客户端只能读取，写入由受控的手动导入操作完成。数据的新旧以数据集自身的最新日期判断，不以 Supabase 的刷新日期判断。

本大学项目在 Flutter 开发前一次性导入所需政府数据，直至项目完成不再导入或更新。可使用不改变镜像业务列的辅助约束或暂存机制完成导入、去重和替换。

Flutter 不直接查询镜像表，而是通过只读 View 或 RPC 读取稳定的公共数据接口。

## 项目自有表

为 LocateMY 业务而设的 Supabase 表，不是政府数据集的镜像。它包括账户私有数据、用户创建且可能公开的数据，以及应用内部所需的数据；每类数据必须按其可见范围施加独立权限规则。

账户私有数据仅归属账户本人。隐患报告由登录账户创建、所有应用用户可读，且仅创建账户可编辑或删除。房产实勘照片的文件存于 Supabase Storage，表中仅保存其归属与说明等元数据，并按实勘的账户归属限制访问。

公开隐患不设维护者审核、隐藏或删除的例外。

## 收藏地点

收藏地点是账户命名保存的地理坐标，不是行政区标识。它以 Supabase 为权威来源；本机仅保存按账户隔离的缓存和离线创建队列，并在前台执行双向同步。

## 隐患报告处理状态

隐患报告处理状态是报告创建者对自身报告的 `pending` 或 `resolved` 标记，不表示平台核验、审核、隐藏或删除决定。

## 房产风险快照

房产风险快照是在创建实勘或改变实勘坐标时记录的统计州、州级安全指数和附近隐患数，具有其采集时间。它保留看房当时的上下文；用户可显式刷新，不能静默覆盖历史记录。

## 本地数据库

手机上的可清理存储，仅保存公共数据缓存和离线待同步队列；不作为用户业务记录的权威来源。用户业务记录以 Supabase 为权威来源，并按账户隔离。

退出或切换账户时，必须清除旧账户的离线队列与私有缓存；不含个人信息的公共政府数据缓存可保留。

离线队列第一版仅支持创建；编辑和删除必须在线完成。公共缓存保存所读数据集的最新日期；在线优先替换为更近期数据，离线可继续展示旧缓存及其数据集日期。

## 评估偏好

**评估偏好（assessment preferences）**：
账户级的五项优先级：安全、成本、日常便利、公共交通可达性和基础设施；它们用于个人化地点适配度，不改变单项分析的原始读数。“日常便利”只作为评估偏好中的维度名称，其读数是 2 公里五类设施覆盖；面向用户的设施分析页面统一称“周边设施”。
_避免_：ICI 权重、单次对比的临时权重

## 当前评估预案

**当前评估预案（current assessment scenario）**：
账户在多份预算预案中选中并保存为当前使用的那一份；它是预算压力和个人化地点适配度的必要输入，账户同一时间只有一份。
_避免_：临时月支出、默认预算

## 个人化地点适配度

**个人化地点适配度（personalized location suitability）**：
基于账户评估偏好、当前评估预案和可用地点分析结果的 0–100 个人化读数，不是客观宜居评分、官方评级或自动推荐。
