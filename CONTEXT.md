---
kind: domain-context
language: zh-CN
canonical: true
---

# LocateMY 领域词汇

本文件只记录已确认的产品术语；功能公式和数据集事实分别以产品知识库的功能文件为准。

## 地点分析范围

一个地点由用户选择的坐标确定。公共交通覆盖范围是该坐标周围 1.5 公里的固定半径，周边设施覆盖范围是该坐标周围 2 公里的固定半径；治安使用地点所属警区，地区统计使用地点所属行政区。

## 行政区与警区

行政区（district）是收入、供水、供电、医疗、教育等地区统计使用的行政统计单位；警区（police district）是犯罪统计使用的警察统计单位。两者不是同一地理概念，分析结果必须标明实际口径。

## 周边设施覆盖

周边设施覆盖是 2 公里范围内已收录 OSM 设施的类别和数量读数，不是设施质量、营业状态或政府服务水平评价。覆盖类别未知时不能当作未覆盖。

## 公共交通覆盖

公共交通覆盖是站点和有效服务路线的相对覆盖读数，不是实际通勤便利或服务质量评价。交通连通性分数供公共交通页面和 ICI 共用。

## 站点实体

通常指 GTFS `stops.txt` 中可上下车的 `location_type = 0` 记录。`parent_station` 可用于展示分组，但不改变站点实体计数。

## 有效路线

通过 `routes.txt`、`trips.txt` 和 `stop_times.txt` 关联到分析范围内站点，并在分析日期由 `calendar.txt` 或 `calendar_dates.txt` 确认有效的路线。

## GTFS Feed 快照

某次取得的官方 GTFS Static ZIP、采集时间、解析状态和有效服务日期。可读取但较旧的快照仍可展示，并标记可能过期；解析失败或预期 feed 缺失时，交通分不可用。

## 评估偏好

评估偏好是账户级的五项优先级：安全、成本、日常便利、公共交通可达性和基础设施；它们用于个人化地点适配度，不改变单项分析的原始读数。

## 个人化地点适配度

个人化地点适配度是基于账户评估偏好、当前评估预案和可用地点分析结果的 0–100 个人化读数，不是客观宜居评分、官方评级或自动推荐。
