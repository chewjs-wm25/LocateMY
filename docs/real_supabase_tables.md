## 数据库表与 RLS 概览

已检查 `public` Schema，共发现 **21 张表**。

---

## 1. `public.spatial_ref_sys`

**RLS：未启用**

```sql
CREATE TABLE public.spatial_ref_sys (
    srid integer PRIMARY KEY
        CHECK (srid > 0 AND srid <= 998999),
    auth_name varchar,
    auth_srid integer,
    srtext varchar,
    proj4text varchar
);
```

- 约 8,500 行
- 主要用于 PostGIS 空间参考系定义

---

## 2. `public.price_catcher`

**RLS：已启用**

```sql
CREATE TABLE public.price_catcher (
    date date NOT NULL,
    premise_code smallint NOT NULL,
    item_code smallint NOT NULL,
    price real NOT NULL,

    PRIMARY KEY (date, premise_code, item_code)
);
```

- 约 151,400 行
- 注释：每日微观物价数据

---

## 3. `public.lookup_item`

**RLS：已启用**

```sql
CREATE TABLE public.lookup_item (
    item_code smallint GENERATED BY DEFAULT AS IDENTITY
        PRIMARY KEY,
    item text NOT NULL,
    unit text NOT NULL,
    item_group text NOT NULL,
    item_category text NOT NULL
);
```

- 约 756 行
- 注释：商品信息查找表

---

## 4. `public.lookup_premise`

**RLS：已启用**

```sql
CREATE TABLE public.lookup_premise (
    premise_code smallint GENERATED BY DEFAULT AS IDENTITY
        PRIMARY KEY,
    premise text NOT NULL,
    address text NOT NULL,
    premise_type text NOT NULL,
    state text NOT NULL,
    district text NOT NULL
);
```

- 约 3,344 行
- 注释：零售场所查找表

---

## 5. `public.cpi_state`

**RLS：已启用**

```sql
CREATE TABLE public.cpi_state (
    state text NOT NULL,
    date date NOT NULL,
    division text NOT NULL,
    index real NOT NULL,

    PRIMARY KEY (state, date, division)
);
```

- 约 44,576 行
- 注释：州级消费物价指数

---

## 6. `public.cpi_core`

**RLS：已启用**

```sql
CREATE TABLE public.cpi_core (
    date date NOT NULL,
    division text NOT NULL,
    index real NOT NULL,

    PRIMARY KEY (date, division, index)
);
```

- 约 1,339 行
- 注释：核心消费物价指数

---

## 7. `public.crime_stats`

**RLS：已启用**

```sql
CREATE TABLE public.crime_stats (
    date date NOT NULL,
    state text NOT NULL,
    district text NOT NULL,
    category text NOT NULL,
    type text NOT NULL,
    crimes integer NOT NULL,

    PRIMARY KEY (date, state, district, category, type)
);
```

- 约 19,152 行

---

## 8. `public.hh_income_district`

**RLS：已启用**

```sql
CREATE TABLE public.hh_income_district (
    state text NOT NULL,
    district text NOT NULL,
    date date NOT NULL,
    income_mean integer NOT NULL,
    income_median integer NOT NULL,

    PRIMARY KEY (state, district, date)
);
```

- 约 480 行
- 注释：县级家庭中位数收入与基尼系数

---

## 9. `public.hh_access_amenities`

**RLS：已启用**

```sql
CREATE TABLE public.hh_access_amenities (
    state text NOT NULL,
    district text NOT NULL,
    date date NOT NULL,
    piped_water real NOT NULL,
    sanitation real NOT NULL,
    electricity real NOT NULL,

    PRIMARY KEY (state, district, date)
);
```

- 约 655 行
- 注释：基础设施普及率，包括供水、卫生设施和电力

---

## 10. `public.hospital_beds`

**RLS：已启用**

```sql
CREATE TABLE public.hospital_beds (
    date date NOT NULL,
    state text NOT NULL,
    district text NOT NULL,
    type text NOT NULL,
    beds integer NOT NULL,

    PRIMARY KEY (date, state, district, type)
);
```

- 约 5,468 行
- 注释：医疗床位数据

---

## 11. `public.enrolment_school_district`

**RLS：已启用**

```sql
CREATE TABLE public.enrolment_school_district (
    date date NOT NULL,
    state text NOT NULL,
    district text NOT NULL,
    stage text NOT NULL,
    sex text NOT NULL,
    students bigint NOT NULL,

    PRIMARY KEY (date, state, district, stage, sex)
);
```

- 约 12,756 行
- 注释：学校在校生人数

---

## 12. `public.teachers_district`

**RLS：已启用**

```sql
CREATE TABLE public.teachers_district (
    date date NOT NULL,
    state text NOT NULL,
    district text NOT NULL,
    stage text NOT NULL,
    sex text NOT NULL,
    teachers bigint NOT NULL,

    PRIMARY KEY (date, state, district, stage, sex)
);
```

- 约 8,805 行
- 注释：教师人数统计

---

## 13. `public.profiles`

**RLS：已启用**

```sql
CREATE TABLE public.profiles (
    id uuid PRIMARY KEY,
    username text NOT NULL UNIQUE
        CHECK (char_length(username) >= 3),
    avatar_url text,
    bio text,
    updated_at timestamptz DEFAULT now(),

    CONSTRAINT profiles_id_fkey
        FOREIGN KEY (id)
        REFERENCES auth.users (id)
);
```

- 约 1 行
- `id` 关联 `auth.users(id)`
- 被以下用户数据表作为外键目标：

```text
public.user_budget_scenarios.user_id
public.crowdsourced_hazards.user_id
public.user_saved_regions.user_id
public.user_ici_preferences.user_id
public.user_property_inspections.user_id
```

---

## 14. `public.user_budget_scenarios`

**RLS：已启用**

```sql
CREATE TABLE public.user_budget_scenarios (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id uuid NOT NULL,
    scenario_name text NOT NULL,
    max_rent numeric DEFAULT 0,
    living_expenses numeric DEFAULT 0,
    transport_allowance numeric DEFAULT 0,
    created_at timestamptz DEFAULT now(),
    updated_at timestamptz DEFAULT now(),

    CONSTRAINT user_budget_scenarios_user_id_fkey
        FOREIGN KEY (user_id)
        REFERENCES public.profiles (id)
);
```

- 当前约 0 行

---

## 15. `public.crowdsourced_hazards`

**RLS：已启用**

```sql
CREATE TABLE public.crowdsourced_hazards (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id uuid,
    hazard_type text NOT NULL,
    title text,
    description text,
    district text,
    location geometry NOT NULL,
    status text DEFAULT 'pending'
        CHECK (
            status IN (
                'pending',
                'verified',
                'resolved',
                'rejected'
            )
        ),
    report_time timestamptz DEFAULT now(),

    CONSTRAINT crowdsourced_hazards_user_id_fkey
        FOREIGN KEY (user_id)
        REFERENCES public.profiles (id)
);
```

- 约 8 行
- 使用 PostGIS `geometry` 类型

---

## 16. `public.user_saved_regions`

**RLS：已启用**

```sql
CREATE TABLE public.user_saved_regions (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id uuid NOT NULL,
    district_id text NOT NULL,
    alias text,
    created_at timestamptz DEFAULT now(),

    CONSTRAINT user_saved_regions_user_id_fkey
        FOREIGN KEY (user_id)
        REFERENCES public.profiles (id)
);
```

- 约 1 行

---

## 17. `public.user_ici_preferences`

**RLS：已启用**

```sql
CREATE TABLE public.user_ici_preferences (
    user_id uuid PRIMARY KEY,
    weight_safety numeric DEFAULT 0.2,
    weight_transit numeric DEFAULT 0.2,
    weight_education numeric DEFAULT 0.2,
    weight_health numeric DEFAULT 0.2,
    weight_amenity numeric DEFAULT 0.2,
    updated_at timestamptz DEFAULT now(),

    CONSTRAINT user_ici_preferences_user_id_fkey
        FOREIGN KEY (user_id)
        REFERENCES public.profiles (id)
);
```

- 当前约 0 行

---

## 18. `public.user_property_inspections`

**RLS：已启用**

```sql
CREATE TABLE public.user_property_inspections (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id uuid NOT NULL,
    property_name text NOT NULL,
    address text,
    rating integer
        CHECK (rating >= 1 AND rating <= 5),
    inspection_data jsonb DEFAULT '{}'::jsonb,
    image_urls text[] DEFAULT '{}'::text[],
    created_at timestamptz DEFAULT now(),

    CONSTRAINT user_property_inspections_user_id_fkey
        FOREIGN KEY (user_id)
        REFERENCES public.profiles (id)
);
```

- 当前约 0 行

---

## 19. `public.transit_stops`

**RLS：已启用**

```sql
CREATE TABLE public.transit_stops (
    stop_id text PRIMARY KEY,
    stop_name text NOT NULL,
    transit_type text NOT NULL,
    latitude double precision NOT NULL,
    longitude double precision NOT NULL,
    geom geometry
);
```

- 约 8,253 行
- 使用 PostGIS `geometry` 类型

---

## 20. `public.police_districts_boundary`

**RLS：已启用**

```sql
CREATE TABLE public.police_districts_boundary (
    id text PRIMARY KEY,
    name text NOT NULL,
    state text NOT NULL,
    boundary_geom geometry
);
```

- 约 160 行
- 使用 PostGIS `geometry` 类型

---

## 21. `public.gdp_gni_annual_real`

**RLS：已启用**

```sql
CREATE TABLE public.gdp_gni_annual_real (
    series text NOT NULL,
    date date NOT NULL,
    gdp double precision NOT NULL,
    gni double precision NOT NULL,
    gdp_capita double precision NOT NULL,
    gni_capita double precision NOT NULL,

    PRIMARY KEY (series, date)
);
```

- 约 111 行
- 注释：年度实际 GDP、GNI 及人均指标时间序列

---

## 22. `public.lfs_month`

**RLS：已启用**

```sql
CREATE TABLE public.lfs_month (
    date date PRIMARY KEY,
    lf real NOT NULL,
    lf_employed real NOT NULL,
    lf_unemployed real NOT NULL,
    lf_outside real NOT NULL,
    u_rate real NOT NULL,
    p_rate real NOT NULL,
    ep_ratio real NOT NULL
);
```

- 约 198 行
- 注释：月度劳动力市场统计数据

---

## RLS 状态汇总

### 已启用 RLS 的表

除 `public.spatial_ref_sys` 外，其余表均已启用 RLS：

```text
public.price_catcher
public.lookup_item
public.lookup_premise
public.cpi_state
public.cpi_core
public.crime_stats
public.hh_income_district
public.hh_access_amenities
public.hospital_beds
public.enrolment_school_district
public.teachers_district
public.profiles
public.user_budget_scenarios
public.crowdsourced_hazards
public.user_saved_regions
public.user_ici_preferences
public.user_property_inspections
public.transit_stops
public.police_districts_boundary
public.gdp_gni_annual_real
public.lfs_month
```

### 未启用 RLS 的表

```text
public.spatial_ref_sys
```