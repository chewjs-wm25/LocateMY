# 模型层：账户组件

## 实体
- `User`：
    - `id`：字符串。
    - `email`：字符串。
    - `avatarUrl`：字符串。
- `UserPreferences`：
    - `defaultLocation`：字符串。
    - `iciWeights`：权重映射。
    - `themeMode`：主题模式。

## DTO
- `SupabaseUserDTO`。
- `ProfileTableDTO`。
