# 交通补充资料导入结果

2026-09-18，用户授权将下载目录中的三个 ZIP 导入当前 Supabase 项目 `ntlhjfljkjeefzzqutbc`，已完成。

| 本地文件 | 确认来源 | 有效服务日期 |
| --- | --- | --- |
| `gtfs.zip` | BAS.MY 怡保（TCBMYBIPH） | 2026-09-18 |
| `gtfs (1).zip` | BAS.MY 芙蓉 A（MYBUSKR） | 2026-09-18 |
| `gtfs (2).zip` | BAS.MY 芙蓉 B（MYBUSGOPI） | 2026-09-18 |

新快照 `manual-2026-09-18-supplement` 已启用。其他 13 个来源从先前已验证快照复制，保留原采集时间及来源身份；原快照保留。没有改变表结构、权限或 RLS，也没有延长官方服务日期。

新快照包含 16 个 feed、17,611 个站点、452 条路线、42,707 条服务日期记录、44,823 条站点/路线/服务关联。五张标准化表与本地准备结果逐条核验通过（经纬度比较至小数点后 10 位），文件 SHA256 与解析结果在证据文件中保留。

重新生成并导入 2026-09-18 的固定参照网格，共 10,271 点；服务范围外点、非正密度和负路线数均为零。完整网格及审计 hash 在推广前验证通过。

以 authenticated 数据库角色调用应用的 `read_transit_analysis`，吉隆坡中心坐标 3.139, 101.6869 当天返回：

- 状态 available，公式 full_formula，交通分 **93/100**。
- 1.5 公里内 49 个站点、24 条有效路线，最近站约 192 米。
- 密度百分位约 96.00，路线百分位约 99.90；沿用 50% 距离、25% 密度、25% 路线原公式。

先前 87/100 是没有参照网格时的距离项部分分数；当前 93/100 已包含密度和路线项。不同坐标的分数可能不同。

三个补充文件只覆盖 2026-09-18；2026-09-19 或其他日期不能由这些 ZIP 确认有效服务，仍可能返回部分分数。关丹保留此前可读取的旧快照，按既有规则标记 stale，并计入可用来源。没有执行每日更新或自动同步。

[导入与校验证据](evidence/transit-supplement-2026-09-18/import-verification.json)。完整本地准备资料保留于 `build/transit-supplement-2026-09-18/`；原始下载 ZIP 未提交。

本次任务为数据导入，数据库及查询验证通过；未修改 Flutter 代码，也未重跑设备页面流程。应用刷新后可读取新快照，本次不重新判定完整模块或 Wave 的验收状态。
