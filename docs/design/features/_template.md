# <Feature 名称>

> 状态：`Draft`
> Owner：`<项目负责人确认的学生>`
> 系统基线：`<commit 或版本>`
> 依赖波次：`<编号>`
> 最后更新：`YYYY-MM-DD`
> Prototype 视觉参考：`<分支、文件或 N/A>`

本文件是协调 Feature 与其消费者的高层设计。它固定可观察成果、模块责任、跨边界契约、受控文件边界和验收情景；Feature 内部的类型、文件拆分、运行时策略与测试组织归实现 Owner。

## 1. 用户成果与范围

- 用户成果：
- 包含的 Capability ID：
- 不包含及原因：
- 产品事实源：
- 原型差异：产品事实和已批准设计优先；列出需要修改的原型表现。

## 2. 依赖、责任与文件边界

| 模块 / 文件边界 | Owner | 负责 | 不负责 | 与其他模块的沟通 |
| --- | --- | --- | --- | --- |
| | | | | |

只列公开入口、共享 composition root、迁移或其他跨 Owner 的文件。每个 Feature 可拥有一个内部目录；Owner 可在其中增补、合并或拆分实现文件，只要不改变本文件的可观察契约。

### 依赖与未决项

| 依赖或问题 | 影响 | 验证方式 / 最迟解决点 |
| --- | --- | --- |
| | | |

## 3. 对外协调契约

### 提供

| ID | 消费者 | 动作与可观察事实 | 输入、结果与失败语义 | 权限与副作用边界 |
| --- | --- | --- | --- | --- |
| `<OWNER>-001` | | | | |

### 消费

| ID | Owner | 使用目的 | 调用方依赖的结果与失败语义 |
| --- | --- | --- | --- |
| | | | |

契约描述动作族、领域事实与可观察结果，不规定语言级符号、参数名、DTO、内部事件、超时、重试、去重、取消或 Adapter 策略。只有跨模块可见的安全、账户、幂等或副作用不变量写入本节。

## 4. 用户可观察行为与跨模块流程

| 入口或用户动作 | 成功结果 | 空、不可用或失败结果 | 必须保持的可访问性 / 安全语义 |
| --- | --- | --- | --- |
| | | | |

对涉及多个 Owner 的流程，使用简短步骤说明各方的责任与完成条件。页面内部状态迁移、Widget 结构和具体文案由实现 Owner 决定。

## 5. 数据与确定性业务规则

| 目的 | 权威对象或事实源 | 访问 / 应用边界 | 必须保持的语义 |
| --- | --- | --- | --- |
| | | | |

数据字段、RLS、迁移和公式正文分别只在 Schema Catalog 或产品知识库定义。本文件列出实际访问对象，以及必须应用的公式/规则 ID、输入输出口径、单位、边界和结果语义。

## 6. 验收与 Ready Gate

| Capability | 验收情景 | 用户操作 | 可观察结果 |
| --- | --- | --- | --- |
| | | | |

- [ ] 每项 Capability 可追踪到 Owner、协调契约、数据/事实源与验收情景。
- [ ] 跨 Owner 的责任、文件边界和可观察副作用没有冲突。
- [ ] 需要固定的公式和业务规则链接至唯一事实源，并写明应用口径。
- [ ] 非正常用户结果及适用的权限、账户切换、离线或安全边界已覆盖。
- [ ] 独立审查发现已关闭，或由设计 AI 依 ADR 0013 明确接受。
- [ ] 阻塞问题已关闭；设计 AI 已依 ADR 0013 批准 `Ready for Development`。

## 7. Change Log

| 日期 | 状态 | 变更原因 | 受影响的 Capability / Interface / 数据对象 / Feature | 批准者 |
| --- | --- | --- | --- | --- |
| | | | | |
