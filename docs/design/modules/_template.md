# <共享模块名称>

> 状态：`Draft`
> Owner：`<项目负责人确认的学生>`
> 系统基线：`<commit 或版本>`
> 消费 Feature：`<Feature...>`
> 最后更新：`YYYY-MM-DD`

本文件协调共享模块与其消费者。它固定共享必要性、可观察契约、受控文件边界和验收情景；模块内部的类型、文件拆分、运行时策略和测试组织归 Owner。

## 1. 建立理由、责任与文件边界

- 跨 Feature 共享的状态、规则、数据访问或公开契约：
- 不能留在单一 Feature 内的原因：
- 明确不拥有的责任：
- 关联 Capability 与系统基线章节：

| 模块 / 文件边界 | Owner | 负责 | 不负责 | 与消费者的沟通 |
| --- | --- | --- | --- | --- |
| | | | | |

只列公开入口、共享 composition root、迁移或其他跨 Owner 文件。Owner 可在模块内部目录自行组织实现文件，不得改变本文件的可观察契约。

## 2. 消费者、依赖与协调契约

| 消费 Feature | 使用目的 | 依赖的 Interface / 事实源 | 所需结果与安全边界 |
| --- | --- | --- | --- |
| | | | |

### 提供

| ID | 消费者 | 动作与可观察事实 | 输入、结果与失败语义 | 权限与副作用边界 |
| --- | --- | --- | --- | --- |
| `<OWNER>-001` | | | | |

契约描述动作族、领域事实与可观察结果。语言级成员、内部事件、超时、重试、去重、取消、Adapter 和测试策略由 Owner 封装；只记录跨 Owner 可见的不变量。

## 3. 数据、确定性规则与用户结果

| 目的 | 权威对象或事实源 | 访问 / 应用边界 | 必须保持的语义 |
| --- | --- | --- | --- |
| | | | |

公式正文、数据字段、RLS 和迁移分别只在产品知识库或 Schema Catalog 维护。本文件记录实际访问对象，以及公式/规则 ID、应用口径和可观察结果。

| 用户或消费者动作 | 成功结果 | 空、不可用或失败结果 | 必须保持的安全 / 可访问性语义 |
| --- | --- | --- | --- |
| | | | |

## 4. 验收与 Ready Gate

| Capability / 消费者 | 验收情景 | 操作 | 可观察结果 |
| --- | --- | --- | --- |
| | | | |

- [ ] 共享必要性、消费者、Owner 和受控文件边界明确。
- [ ] 协调契约、数据/事实源与可观察副作用均有单一来源。
- [ ] 需要固定的规则链接至唯一事实源，并写明应用口径。
- [ ] 阻塞问题归零；独立审查完成；项目负责人已批准 `Ready for Development`。

## 5. Change Log

| 日期 | 状态 | 变更原因 | 影响的 Feature / Interface / 数据对象 | 批准者 |
| --- | --- | --- | --- | --- |
| | | | | |
